[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "placas"
version = "0.1.0"
description = "Control de acceso vehicular con pico y placa (proyecto didactico)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
